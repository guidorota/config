#!/bin/bash

case $1 in
    start)
    VBoxManage startvm ubuntu --type headless
    ssh -p 3022 sidewinder@localhost
    ;;
    login)
    ssh -p 3022 sidewinder@localhost
    ;;
    stop)
    VBoxManage controlvm ubuntu poweroff
    ;;
    *)
    echo "vm [start|login|stop]"
    ;;
esac
